import React from 'react';

export function Profile() {
    return (
        // Add logic to conditionally render page based on loginState;
        <div>
            <h1>User Profile / Login Page to be made</h1>
        </div>    
    );
};

export default Profile;